{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "mindforge://schemas/actions/enforcement_adjacent_decision_record.v1.json",
  "title": "Enforcement Adjacent Decision Record v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "kind",
    "version",
    "canonical_action_hash",
    "enforcement_adjacent_decision",
    "deterministic",
    "enforcing"
  ],
  "properties": {
    "kind": { "const": "enforcement_adjacent_decision_record" },
    "version": { "const": "v1" },
    "canonical_action_hash": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "enforcement_adjacent_decision": {
      "type": "object",
      "additionalProperties": false,
      "required": ["decision", "readiness", "bridge_verdict", "reasons"],
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["would_allow", "would_review", "would_deny", "insufficient_signal"]
        },
        "readiness": {
          "type": "string",
          "enum": ["ready", "not_ready", "unknown"]
        },
        "bridge_verdict": {
          "type": "string",
          "enum": ["allow", "review", "deny", "unknown"]
        },
        "reasons": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kind", "message"],
            "properties": {
              "kind": { "type": "string", "minLength": 1 },
              "message": { "type": "string", "minLength": 1 },
              "rule_id": { "type": "string" }
            }
          }
        }
      }
    },
    "deterministic": { "const": true },
    "enforcing": { "const": false }
  }
}
